node_modules/
dist/

.env
.env.*
!.env.example

.DS_Store
.vs
.vscode
.idea
.history

# sqlite payment store
data/*.db
data/*.db-shm
data/*.db-wal
data/*

# logs
*.log

# Fireblocks secrets (PEM files, keys)
secrets/*

# Facilitator config (contains Fireblocks API key + per-config secrets)
config/facilitator.json

# MCP server config (may contain API keys)
.mcp.json
